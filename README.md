# aihs-attendance-backend

Backend service for the AIHS Attendance System.

## Stack
- Python / FastAPI
- SQLite / PostgreSQL
- REST API

## Setup
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
